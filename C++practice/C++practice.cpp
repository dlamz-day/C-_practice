#include <iostream>
#include <stdio.h>


using namespace std;

int main()
{
    cout << "//below is Calculate 'e' by 1+1/1! + 1/2! + 1/3! +...+ 1/n!; n defined by user. //////" << endl ;
/*  double Sum1 = 1;
    double Sum2 =1;
    int a= 1, n;
    cout << "enter the number: ";
    cin  >>  n;
    while(a < n ){
       Sum1 *= a;
       a++;
       Sum2 += (1/Sum1);
       printf("Now sum = %f , a=%d  \n" , Sum2, a);
    }
    */





/*    cout << "//below is sorting, Find the biggest by myself. //////" << endl ;

    int n, i=0;
    int biggest, input1, input2;
    cout << "select the biggest, enter the number (first one is the number of value remaining, 0 for end entering) " << endl;
    cin  >> n >> biggest;
    while (i < n-1){
        i++;
        cin >> input2;
        if ( biggest == 0 || input2 == 0){
            break;
        }
        else{
            if(biggest < input2 ){ biggest = input2;}
        //  else{biggest = biggest;}
        }
        cout << "now biggest is :" << biggest << endl;
    }
    cout << "final biggest is :" << biggest << endl;

*/








/* Below print for the triangular shape of stars.
     int a,b;

      for(b=1;b<=10;b++){
        for(a=1;a<b;a++){
        cout<<" ";
        }
//       cout << a <<endl ;
        for(a=10;a>b;a--){
         cout<<"*";
        }
       cout<<"\n";
      }

    return 0;
}


*/
//
